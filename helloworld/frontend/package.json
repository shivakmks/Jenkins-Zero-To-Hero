{
  "name": "my-frontend-app",
  "version": "1.0.0",
  "description": "Simple node project for Jenkins practice",
  "main": "helloworld.js",
  "dependencies": {
    "chalk": "^4.1.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "build": "echo \"Building frontend...\" && mkdir -p dist && cp helloworld.js dist/"
  }
}
